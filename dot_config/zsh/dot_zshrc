# shellcheck shell=bash
# shellcheck disable=SC1091
# filetype=sh

export GPG_TTY=$(tty)
FW_MONOREPO_HOME="$HOME/workspace/flywheel-jp/monorepo"
# Flywheel specific utilities
[[ -f "$HOME/.zshrc.flywheel" ]] && source "$HOME/.zshrc.flywheel"
[[ -f "$FW_MONOREPO_HOME/tools/shell/fzf.sh" ]] && source "$FW_MONOREPO_HOME/tools/shell/fzf.sh"

# Load my utilities
[[ -f "$ZDOTDIR/src/init.zsh" ]] && source "$ZDOTDIR/src/init.zsh"
