; Ported from dot_config/private_pet/snippet.toml.

% homebrew

# List of formula packages
brew info --json=v2 --installed | jq -r '.formulae[] | select(.installed[0].installed_on_request == true) | .name'
