# =============================================================================
# GlazeWM — modifier grammar
# -----------------------------------------------------------------------------
#   alt                  + arrows    focus window in direction
#   alt + shift          + h/j/k/l   move window in direction
#   alt + control        + h/j/k/l   resize focused window
#   alt + control+shift  + h/j/k/l   move whole workspace to monitor in direction
#   alt + control        + 1..9      focus workspace N
#   alt + shift          + 1..9      send focused window to workspace N (follows)
# -----------------------------------------------------------------------------
# The Control modifier MUST be spelled `control`. GlazeWM accepts only
# shift/lshift/rshift, control/lctrl/rctrl, alt/lalt/ralt, lwin/rwin — `ctrl` is
# not among them, and an unrecognised modifier is dropped SILENTLY: the config
# parses, errors.log stays empty, and the binding simply never fires. This file
# shipped with `alt+ctrl+h/j/k/l` for resize, which therefore never worked.
# -----------------------------------------------------------------------------
# RESERVED: plain `alt` + a bare letter or digit belongs to tmux, not to GlazeWM.
#
# GlazeWM registers its bindings at the OS level, so whatever it claims never
# reaches the focused application at all. tmux.conf drives nearly everything off
# prefix-less M-* bindings, and this file used to sit on plain `alt` — which
# silently ate M-h/j/k/l (pane nav), M-v/M-t (splits), M-s/M-d (session switch,
# detach), M-1..9 (window select), M-m (copy mode) and M-r. The failure mode is
# nasty: the key produces nothing at all, so it reads as "tmux is broken" rather
# than as a clash between two programs.
#
# Hence the grammar above. Everything GlazeWM owns carries `control` or `shift`
# alongside `alt`, or uses an arrow key — none of which tmux binds. The only
# plain-`alt` bindings left here are the four arrows and `alt+enter`.
#
# Do NOT "simplify" a binding back to plain alt+<letter> or alt+<digit> without
# checking `tmux list-keys -T root` first.
#
# (An earlier attempt split the two Option keys instead — GlazeWM on `ralt`,
# tmux on `lalt`. GlazeWM's docs list `lalt`/`ralt` as valid and the config
# parsed without error, but its macOS build ignores the left/right distinction:
# `ralt+v` still fired on the LEFT Option key. Do not retry that approach.)
# =============================================================================

general:
  # Commands to run when the WM starts (e.g. launch a bar or script).
  startup_commands: []
  focus_follows_cursor: false
  toggle_workspace_on_refocus: false
  cursor_jump:
    enabled: true
    # 'window_focus': warp cursor on any focus change. 'monitor_focus': only
    # when focus crosses monitors.
    trigger: "window_focus"

gaps:
  inner_gap: "10px"
  outer_gap:
    top: "5px"
    right: "5px"
    bottom: "5px"
    left: "5px"

window_effects:
  # Border effects are Windows 11 only (API limitation).
  focused_window:
    border:
      enabled: true
      color: "#bea3c7"
  other_windows:
    border:
      enabled: true
      color: "#a1a1a1"

window_behavior:
  # New windows: 'tiling' or 'floating'.
  initial_state: "tiling"
  state_defaults:
    floating:
      centered: true
      shown_on_top: false
    fullscreen:
      # Maximize if the window supports it, else true fullscreen.
      maximized: false
      shown_on_top: false

workspaces:
  - name: "1"
  - name: "2"
  - name: "3"
  - name: "4"
  - name: "5"
  - name: "6"
  - name: "7"
  - name: "8"
  - name: "9"

window_rules:
  # Leave these unmanaged (never tiled).
  - commands: ["ignore"]
    match:
      # Browser picture-in-picture popups.
      - window_title: { regex: "[Pp]icture.in.[Pp]icture" }
        window_class: { regex: "Chrome_WidgetWin_1|MozillaDialogClass" }
      # Windows 11 Command Palette.
      - window_title: { equals: "Command Palette" }
        window_class: { equals: "WinUIDesktopWin32WindowClass" }
      # Lively Wallpaper render surfaces.
      - window_process: { equals: "Lively" }
        window_class: { regex: "HwndWrapper*" }
      # WezTerm (leave unmanaged/floating).
      - window_process: { equals: "wezterm-gui" }

binding_modes:
  # alt+control+r → resize with h/j/k/l or arrows; enter/escape exits.
  - name: "resize"
    keybindings:
      - commands: ["resize --width -2%"]
        bindings: ["h", "left"]
      - commands: ["resize --width +2%"]
        bindings: ["l", "right"]
      - commands: ["resize --height +2%"]
        bindings: ["k", "up"]
      - commands: ["resize --height -2%"]
        bindings: ["j", "down"]
      - commands: ["wm-disable-binding-mode --name resize"]
        bindings: ["escape", "enter"]
  # alt+shift+p → disable all binds (e.g. fullscreen games); alt+shift+p exits.
  - name: "pause"
    keybindings:
      - commands: ["wm-disable-binding-mode --name pause"]
        bindings: ["alt+shift+p"]

keybindings:
  # ---------------------------------------------------------------------------
  # Focus window            alt + arrows
  # ---------------------------------------------------------------------------
  - commands: ["focus --direction left"]
    bindings: ["alt+left"]
  - commands: ["focus --direction down"]
    bindings: ["alt+down"]
  - commands: ["focus --direction up"]
    bindings: ["alt+up"]
  - commands: ["focus --direction right"]
    bindings: ["alt+right"]

  # ---------------------------------------------------------------------------
  # Move window             alt + shift + h/j/k/l
  # ---------------------------------------------------------------------------
  - commands: ["move --direction left"]
    bindings: ["alt+shift+h", "alt+shift+left"]
  - commands: ["move --direction down"]
    bindings: ["alt+shift+j", "alt+shift+down"]
  - commands: ["move --direction up"]
    bindings: ["alt+shift+k", "alt+shift+up"]
  - commands: ["move --direction right"]
    bindings: ["alt+shift+l", "alt+shift+right"]

  # ---------------------------------------------------------------------------
  # Resize window           alt + control + h/j/k/l   (mirrors resize mode)
  # ---------------------------------------------------------------------------
  - commands: ["resize --width -2%"]
    bindings: ["alt+control+h"]
  - commands: ["resize --width +2%"]
    bindings: ["alt+control+l"]
  - commands: ["resize --height +2%"]
    bindings: ["alt+control+k"]
  - commands: ["resize --height -2%"]
    bindings: ["alt+control+j"]

  # ---------------------------------------------------------------------------
  # Move workspace → monitor  alt + control + shift + h/j/k/l
  # ---------------------------------------------------------------------------
  - commands: ["move-workspace --direction left"]
    bindings: ["alt+control+shift+h"]
  - commands: ["move-workspace --direction down"]
    bindings: ["alt+control+shift+j"]
  - commands: ["move-workspace --direction up"]
    bindings: ["alt+control+shift+k"]
  - commands: ["move-workspace --direction right"]
    bindings: ["alt+control+shift+l"]

  # ---------------------------------------------------------------------------
  # Binding modes
  # ---------------------------------------------------------------------------
  - commands: ["wm-enable-binding-mode --name resize"]
    bindings: ["alt+control+r"]
  - commands: ["wm-enable-binding-mode --name pause"]
    bindings: ["alt+shift+p"]

  # ---------------------------------------------------------------------------
  # Layout & window state
  # ---------------------------------------------------------------------------
  # macOS only: chains a synthesized Cmd+N with the direction change so a
  # single key both opens a new window and sets where it lands (see
  # new-window.sh). shell-exec runs first because set-tiling-direction can
  # detach the subject container (GlazeWM silently drops any commands
  # chained after it in that case).
  - commands: ["set-tiling-direction vertical"]
    bindings: ["alt+control+v"]
  - commands: ["set-tiling-direction horizontal"]
    bindings: ["alt+shift+v"]
  - commands: ["toggle-floating --centered"]
    bindings: ["alt+shift+space"]
  - commands: ["toggle-tiling"]
    bindings: ["alt+control+t"]
  - commands: ["toggle-fullscreen"]
    bindings: ["alt+control+f"]
  - commands: ["toggle-minimized"]
    bindings: ["alt+control+m"]
  - commands: ["close"]
    bindings: ["alt+shift+q"]

  # ---------------------------------------------------------------------------
  # WM control
  # ---------------------------------------------------------------------------
  - commands: ["wm-reload-config"]
    bindings: ["alt+shift+r"]
  - commands: ["wm-redraw"]
    bindings: ["alt+shift+w"]
  - commands: ["wm-exit"]
    bindings: ["alt+shift+e"]

  # ---------------------------------------------------------------------------
  # Launchers
  # ---------------------------------------------------------------------------
  - commands: ["shell-exec wezterm-gui"]    # WezTerm (matches this repo's term)
    bindings: ["alt+enter"]

  # ---------------------------------------------------------------------------
  # Focus workspace         alt + control + 1..9   (+ control+a/s/d cycling, non-directional)
  # ---------------------------------------------------------------------------
  - commands: ["focus --prev-workspace"]
    bindings: ["alt+control+a"]
  - commands: ["focus --next-workspace"]
    bindings: ["alt+control+s"]
  - commands: ["focus --recent-workspace"]
    bindings: ["alt+control+d"]
  - commands: ["focus --workspace 1"]
    bindings: ["alt+control+1"]
  - commands: ["focus --workspace 2"]
    bindings: ["alt+control+2"]
  - commands: ["focus --workspace 3"]
    bindings: ["alt+control+3"]
  - commands: ["focus --workspace 4"]
    bindings: ["alt+control+4"]
  - commands: ["focus --workspace 5"]
    bindings: ["alt+control+5"]
  - commands: ["focus --workspace 6"]
    bindings: ["alt+control+6"]
  - commands: ["focus --workspace 7"]
    bindings: ["alt+control+7"]
  - commands: ["focus --workspace 8"]
    bindings: ["alt+control+8"]
  - commands: ["focus --workspace 9"]
    bindings: ["alt+control+9"]

  # ---------------------------------------------------------------------------
  # Send window → workspace   alt + shift + 1..9  (moves and follows)
  # ---------------------------------------------------------------------------
  - commands: ["move --workspace 1", "focus --workspace 1"]
    bindings: ["alt+shift+1"]
  - commands: ["move --workspace 2", "focus --workspace 2"]
    bindings: ["alt+shift+2"]
  - commands: ["move --workspace 3", "focus --workspace 3"]
    bindings: ["alt+shift+3"]
  - commands: ["move --workspace 4", "focus --workspace 4"]
    bindings: ["alt+shift+4"]
  - commands: ["move --workspace 5", "focus --workspace 5"]
    bindings: ["alt+shift+5"]
  - commands: ["move --workspace 6", "focus --workspace 6"]
    bindings: ["alt+shift+6"]
  - commands: ["move --workspace 7", "focus --workspace 7"]
    bindings: ["alt+shift+7"]
  - commands: ["move --workspace 8", "focus --workspace 8"]
    bindings: ["alt+shift+8"]
  - commands: ["move --workspace 9", "focus --workspace 9"]
    bindings: ["alt+shift+9"]
